decimals = set()

n = 1; d = 2

while d <= 12000:
    while n < d:
        if 1/2 > n/d > 1/3:
            decimals.add(n/d)
        n += 1
    n = 1
    d += 1

sorted_decimals = sorted(list(decimals))
print(f"Answer to Problem 73: {len(sorted_decimals)}")
# SOLVED: 7295372
